import "./CTA.scss";

const CTA = (props) => {
  return (
    <div className="cta">
      <a href="./CTA.scss" className="btn" download>
        Download CV
      </a>
      <button className="btn btn-primary">Let's Talk</button>
    </div>
  );
};

export default CTA;
